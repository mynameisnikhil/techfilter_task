import { Card, CardContent, Typography } from "@mui/material";

function FiltersPanel() {
  return (
    <Card>
      <CardContent>
        <Typography variant="h6">Filters Panel</Typography>
        {/* Filters will go here */}
      </CardContent>
    </Card>
  );
}

export default FiltersPanel;
